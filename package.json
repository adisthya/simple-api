{
  "name": "react-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "json-server -p 3344 -w ./db/generic.json -r routes.json -d 1000 --nc",
    "start:auth": "json-server -p 3344 -d 1000 -w ./db/generic.json -r routes.json -m ./middlewares/auth.js ./middlewares/login.js --nc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
